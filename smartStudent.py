# Check for smartest student by reading a file with marks, then write a new file 


def smartest_student(h):
    s, d = open(src, "r"), open(dst, "w")
    a = s.readlines
    #a.sort()
    if (a) > 0:
        b = s.readline()
        if b > a:
            c = s.readline()
            while True:
                if b

    s.close(); d.close()



        
for line in a:
    return a[1] * 0.3 + a[2] * 0.7
    for num in line:
        return max(num)
    d.write(max)




with open("StudentScore2.txt", "w") as my_file:
	my_file.write("...")
	if my_file.closed == False:
	    my_file.close()
print my_file.closed
